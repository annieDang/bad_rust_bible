# src/

This is where the sample Rust code lives — the bad examples, the good examples, and everything in between that gets referenced in the book.

Each subdirectory maps to a book/chapter:

```
src/
└── ownership/
    └── clone_thy_neighbour/   ← Book I, Ch. I
```
